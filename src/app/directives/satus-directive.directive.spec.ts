import { SatusDirectiveDirective } from './satus-directive.directive';

describe('SatusDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SatusDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
