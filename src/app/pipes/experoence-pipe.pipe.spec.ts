import { ExperoencePipePipe } from './experoence-pipe.pipe';

describe('ExperoencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExperoencePipePipe();
    expect(pipe).toBeTruthy();
  });
});
